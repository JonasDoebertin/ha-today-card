[test]
preload = ["./tests/support/setup.ts"]
coverageReporter = ["text"]
# The suite's own scaffolding is not the thing being measured.
coveragePathIgnorePatterns = ["**/tests/**"]
coverageSkipTestFiles = true
